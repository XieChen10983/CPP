/*
6.1 函数的形参和实参有什么区别
    （1） 形参变量只有在函数被调用的时候才会分配，调用结束后会立即释放内存。其作用域是在函数内部，不能再函数外部使用。
    （2） 实参可以是常亮、变量、表达式、函数等等。无论是何种类型的数据，在函数调用时必须要有确定的值，因此应该提前赋值。
    （3） 实参和形参在数量上、类型上、顺序上必须严格一致，否则会发生类型不匹配的错误。除非能够进行自动类型的转换。
    （4） 函数调用中发生的数据传递是单向的。（除非使用地址或者引用）
*/