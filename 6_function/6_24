void print(const int ia[10])
{
    for (size_t i = 0; i != 10; ++i)
        cout << ia[i] << endl;
}

描述上述函数的行为。如果存在问题，请指出。

该函数本来是想打印一个长度为10的整型数组的元素。但是并不能达到想要的效果。原因是：
数组传入形参的时候是转换成指针来进行处理的。转换成指针时，数组维度的信息丢失了，将不检查数组的长度。