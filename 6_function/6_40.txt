// 1. 含有默认实参的函数时，尽量让不怎么使用默认值的形参出现在前面，让经常使用默认值的形参出现在后面
// 2. 通常的函数声明习惯放在头文件中。多次声明同一个函数是合法的，但是一个形参只能被赋予一次默认实参。
        例如string screen(sz, sz, char = ' ');之后只能string screen(sz, sz = 80, char);
// (a)是对的，(b)是错的。默认实参必须放在后面。默认参数后面的形参都应该给出默认值。